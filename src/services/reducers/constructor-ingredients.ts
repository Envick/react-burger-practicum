
const initialState = {
    ingredients: []
}

export const constructorReducer = (state = initialState, action: any) => {
    switch(action.type){
        default:{
            return {...state}
        }
    }
}